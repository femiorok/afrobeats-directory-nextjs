import React from 'react'

const Recommendations = ({recommendations}) => {
    console.log(recommendations, "recommend")
  return (
    <div>
        {/* <div className="song-card w-[300px] h-[400px] flex-col justify-between relative">
        <img className="object-cover h-full w-full" src={songSeed1?.trackMetadata.displayImageUri}></img>
        <div className="footer w-full backdrop-blur-sm absolute bottom-0 text-center text-xl font-bold text-slate-100 tracking-wide bg-slate-900/40 px-2">
        <h1>{songSeed1?.trackMetadata.trackName} - {songSeed1?.trackMetadata.artists[0].name}</h1>
        </div>
        </div> */}
    </div>
  )
}

export default Recommendations